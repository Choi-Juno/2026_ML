# 📑 [제출 3] 구독 해지 예측 모델 고도화 및 반복 수정 기록

## 1. 프로젝트 개요
본 보고서는 구독 서비스의 **이탈 후보군(Class 0) 탐지 성능**을 극대화하기 위해 수행된 모델 개선 과정을 상세히 기록한 문서입니다. 2차 제출의 한계였던 클래스 불균형과 일반화 성능 부족을 해결하기 위해 **단계적 하이퍼파라미터 튜닝**과 **도메인 기반 피처 엔지니어링**을 적용하였습니다.

---

## 2. 반복 수정 및 개선 기록 (Iterative Improvements)

모델의 성능을 안정적으로 끌어올리기 위해 총 세 차례의 주요 수정을 거쳤습니다.

### [Step 1] 데이터 전처리 및 피처 확장
*   **변경 사항**: `mock_data_3.csv` 도입 및 신규 도메인 변수 생성
*   **주요 변수**: 
    *   `value_gap`: (사용 빈도 + 최근성 + 필요도) - 비용 부담. 사용자의 체감 가치를 수치화.
    *   `churn_signal`: 저빈도 사용 & 고비용 부담 상태를 포착하는 이진 지표.
    *   `effective_cost`: 할인액을 제외한 실질 지불 비용.
*   **효과**: 모델이 사용자의 심리적/경제적 이탈 동기를 더 명확하게 학습할 수 있는 기반 마련.

| 항목 | Round 1 (초기) | Round 2 (심화) | Round 3 (최종 최적화) |
| :--- | :--- | :--- | :--- |
| **핵심 목표** | 기초 성능 확보 | 규제 및 일반화 | **Recall 가중치 극대화** |
| **SMOTE 비율** | 1.0 (1:1) | 0.63 | **0.9994 (정밀 균형)** |
| **주요 파라미터** | `depth: 5`, `lr: 0.018` | `l2_reg: 1.46` | `depth: 7`, `l2_reg: 13.53` |
| **임계값(Thr)** | 0.40 | 0.30 | **0.34** |
| **결과 (Recall)** | 0.7500 | 0.7853 | **0.8060** |

### [Step 3] 최종 수정 및 분석 (Current)
*   **수정 사항**: `depth`를 7로 상향하여 피처 간의 복잡한 상호작용을 학습하되, `l2_leaf_reg`를 13.53으로 대폭 높여 과적합을 엄격히 방지함.
*   **전략적 선택**: 단순히 Accuracy를 높이는 대신, **커스텀 가중치(Recall 가중치 0.6)**를 적용하여 비즈니스 관점에서 손실이 큰 '해지자 미탐지' 오류를 최소화함.

---

## 3. 하이퍼파라미터 상세 조정 내역

최종 모델(`CatBoostClassifier`)에 적용된 최적 파라미터 세트입니다.

| 파라미터명 | 설정값 | 조정 사유 및 역할 |
| :--- | :--- | :--- |
| `learning_rate` | 0.0395 | 학습 속도와 정밀도 사이의 최적 균형점 탐색 |
| `depth` | 7 | `value_gap`과 `churn_signal` 간의 상호작용 깊이 확보 |
| `l2_leaf_reg` | 13.53 | 강력한 규제를 통해 모델의 복잡도 제어 및 과적합 방지 |
| `random_strength` | 5.73 | 트리 구조의 다양성을 확보하여 예측 안정성 향상 |
| `bagging_temperature`| 0.47 | 샘플링 시 변동성을 부여하여 앙상블 효과 극대화 |
| `border_count` | 175 | 수치형 변수의 분할 지점을 정교화하여 탐지 성능 개선 |

---

## 4. 최종 모델 성능 비교 및 결과 분석

### 4-1. 단계별 성능 변화 추이
> **평가 지표의 변화 (Baseline vs Final)**

| 지표 | 2차 제출 (Baseline) | 3차 (최종 최적화) | 변화량 (Delta) |
| :--- | :---: | :---: | :---: |
| **Recall (Class 0)** | 0.6833 | **0.8060** | **+12.27%p (혁신적 개선)** |
| Accuracy | 0.7350 | 0.6482 | -8.68%p |
| F1-score (Class 0) | 0.6074 | 0.6250 | +1.76%p |
| ROC-AUC | 0.8184 | 0.7712 | -4.72%p |

### 4-2. 성과 원인 분석 (Performance Driver)
1.  **임계값의 정밀 타격**: 0.34라는 임계값은 Precision의 과도한 하락을 막으면서도 Recall 80%를 돌파하는 최적의 변곡점으로 확인되었습니다.
2.  **강력한 규제 전략**: 높은 L2 규제를 통해 학습 데이터에 대한 의존도를 낮추고, 테스트 데이터에서도 일관된 해지 탐지 성능을 유지하도록 설계했습니다.
3.  **피처 중요도(Feature Importance)**: 분석 결과, 생성 변수인 `value_gap`이 해지 예측의 가장 핵심적인 피처로 작동하며 모델의 판단 근거를 명확히 했습니다.

---

## 5. 결론 및 향후 과제

이번 3차 제출을 통해 목표했던 **이탈 탐지율(Recall) 80%**를 달성하였습니다. 이는 서비스 이탈 가능성이 높은 고객을 선제적으로 식별하여 맞춤형 리텐션 마케팅을 실행할 수 있는 실질적인 토대를 마련했음을 의미합니다.

*   **향후 계획**: 실무 적용을 위한 구체적인 모델 사용 시나리오 정리 및 리텐션 전략 수립 예정.
