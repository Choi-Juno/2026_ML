🚀 프로젝트 개요
이 프로젝트는 사용자의 구독 서비스 이용 패턴을 분석하여 '해지 후보(0)'와 '유지 후보(1)'를 분류하는 머신러닝 시스템입니다. 단순한 정적 예측을 넘어, Flask 서버를 통해 실시간 예측을 수행하고 사용자의 피드백을 수집하여 모델을 지속적으로 재학습(Incremental Learning)하는 구조를 가지고 있습니다.

🛠 핵심 기술 스택
Backend: Python (Flask), SQLAlchemy (SQLite)

ML Engine: CatBoost Classifier

Data Science: Pandas, NumPy

Scheduler: APScheduler (자동 재학습 관리)

📂 에이전트를 위한 주요 파일 설명
project_overview.md: 프로젝트의 배경, 파생 변수 산출식, 모델별 성능 비교 지표가 정리되어 있습니다.

predict_server.py: Flask 기반의 추론 및 피드백 수집 서버입니다. 기기 식별(X-Device-Id)과 예측 로그 저장을 담당합니다.

retrain.py: CSV 데이터와 DB의 피드백 데이터를 병합하여 모델을 다시 학습시키는 파이프라인입니다.

db.py: 예측 결과와 사용자 피드백을 저장하기 위한 데이터베이스 스키마 정의 파일입니다.

🧠 도메인 지식 및 피처 엔지니어링 규칙
새로운 기능을 추가하거나 코드를 수정할 때 다음의 피처 엔지니어링 로직을 준수해야 합니다:

Value Gap: 빈도 점수 + 최근성 점수 + 필요도 - 비용 부담. 사용 가치 대비 비용 부담을 나타내는 핵심 지표입니다.

Churn Signal: 이용 빈도와 최근성이 낮고(≤2) 비용 부담이 높은(≥4) 경우를 규칙 기반으로 포착합니다.

Effective Cost: 월 구독료에서 할인 금액을 뺀 실제 지출 금액을 모델 학습에 사용합니다.

🤖 에이전트 가이드라인 (작업 시 주의사항)
1. 코드 작성 및 수정 원칙
Clean Code: 모든 주요 로직에는 한글 주석을 달고, 최신 Python 문법(Type Hinting 등)을 적용하세요.

Error Handling: 서버 엔드포인트 수정 시 적절한 HTTP 상태 코드와 에러 메시지를 반환해야 합니다.

2. 모델 일관성 유지
predict_server.py에서 추론할 때 사용하는 feature_engineer 함수와 retrain.py에서 학습할 때 사용하는 함수는 반드시 동일한 로직을 유지해야 합니다. 피처가 추가될 경우 두 곳 모두 업데이트하세요.

3. 데이터 보안 및 식별
모든 요청은 X-Device-Id 헤더를 통해 기기를 식별합니다. 데이터를 조회하거나 수정하는 로직을 작성할 때 반드시 해당 ID를 필터링 조건에 포함하세요.

4. 재학습 트리거
재학습은 설정된 최소 피드백 개수(RETRAIN_MIN_FEEDBACK)를 충족할 때만 실행됩니다. 테스트 시에는 이 값을 조정하거나 /retrain 엔드포인트를 직접 호출하세요.